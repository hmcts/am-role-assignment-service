package uk.gov.hmcts.reform.roleassignment.domain.service.common;

public class UpdateStatusService {

    // 1. first change the requested role entry in the table
    // 2. once all requested role are approved update status of request approve.
    // 3. once request is approve move requested role to live status.
    // 4. corresponding entry will be inserted into roleassignment live table.
}
